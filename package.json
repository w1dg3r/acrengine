{
  "name": "acrengine",
  "version": "1.0.0",
  "description": "A 2D engine in typescript",
  "main": "src/app.ts",
  "scripts": {
    "build": "tsc",
    "start": "npx http-server .",
    "clean": "rm -rf out",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}